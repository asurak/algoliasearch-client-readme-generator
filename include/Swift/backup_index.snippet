```swift
self.index.browse(query, block: { (iterator, end, error) -> Void in
    if let error = error {
        // Handle errors
    } else if end {
        // End of index
    } else {
        // Do something
        iterator.next()
    }
})
```