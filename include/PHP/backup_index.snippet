```php
// Iterate with a filter over the index
foreach ($this->index->browse('', array("numericFilters" => "i<42")) as $key => $value)
    ;//Do something

$next_cursor = $this->index->browseFrom('', array("numericFilters" => "i<42"), null)["cursor"];
```

